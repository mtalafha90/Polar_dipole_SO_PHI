# Impact of Solar Orbiter Polar-View Magnetic Constraints on Surface Flux Transport Estimates of the Sun's Axial Dipole Moment

*Working manuscript draft. Numbers are measured by the tooling in this
repository; see `RESEARCH_PLAN.md` for the code map and `paper_outline.md`
for the condensed results ledger. Figures are generated by the scripts
named inline.*

## Abstract

The Sun's axial dipole moment g₁₀ is the strongest precursor of the
amplitude of the following solar cycle, and it is the quantity a surface
flux transport (SFT) model must be initialised with. Its value is set by
the polar magnetic fields — precisely the region that near-ecliptic
magnetographs such as SDO/HMI never observe at favourable viewing angles.
We ask whether the out-of-ecliptic vantage of Solar Orbiter/PHI measurably
improves polar-field coverage and the resulting SFT prediction. Using a
common, open-source pipeline that builds PHI-only, HMI-only (native
Earth-view geometry), and merged Carrington maps and computes g₁₀ under
three polar-filling assumptions, we analyse a low-inclination demonstration
window (CR 2264, 2022) and the decisive high-B₀ campaign of February–April
2025 (CR 2294–2297), during which Solar Orbiter's heliographic latitude of
disk centre swept from −2° through −16.7° to +16.8°. We find that PHI's
≥60° polar-cap coverage overtakes HMI by up to a factor of 16 (north cap
51% vs 3% in April), that the advantage switches on with |B₀| exactly as
predicted, and — crucially — that it is largest precisely when the
SolO–Earth separation is largest, so a per-pixel map merge is invalid at
the decisive epochs. We therefore treat PHI as a *standalone polar
constraint* rather than a co-registered merge partner. Injecting PHI's
north-cap field into the SFT initial condition at the April epoch, where
HMI is effectively blind, shifts the first predicted polar reversal by
−1.68 yr; injecting its co-observed south-cap field where HMI already sees
the pole changes nothing. The polar view alters the flux-transport
prediction only where the Earth view lacks the information.

## 1. Introduction

The polar magnetic field near solar minimum is the best-known precursor of
the amplitude of the next sunspot cycle, and its low-order proxy — the
axial dipole moment g₁₀ = (3/4π) ∮ B_r sin(λ) dΩ — is the natural
summary quantity because it is what surface flux transport (SFT) models
propagate forward. Any error in the polar field therefore propagates
directly into cycle-amplitude forecasts.

The polar field is also the hardest part of the Sun to measure. From the
ecliptic the poles are seen at grazing angle (µ = cos of the heliocentric
angle → 0), so the line-of-sight field there is a large and uncertain
extrapolation, and for months around each equinox a pole is tilted away
from Earth entirely. Standard synoptic products (e.g.
`hmi.synoptic_mr_polfil_720s`) fill the polar caps with a temporal/spatial
model rather than a contemporaneous measurement. The polar contribution to
g₁₀ is thus, to a significant degree, an assumption.

Solar Orbiter, whose orbit is progressively inclined out of the ecliptic by
successive Venus gravity assists, carries the Polarimetric and Helioseismic
Imager (PHI) and provides the first opportunity to view the solar poles at
favourable angles from a second vantage. This paper asks a specific,
falsifiable question: **does adding PHI's polar-view magnetic information
measurably improve the estimate of g₁₀ and the SFT prediction that follows
from it, relative to Earth-view data alone?** We answer it with a single
open-source pipeline applied to two windows — a 2022 low-inclination
demonstration and the high-inclination campaign of early 2025 — and arrive
at a reframing of *how* the two vantages are best combined.

## 2. Observations and data preparation

We use SO/PHI-FDT line-of-sight (blos) Level-2 magnetograms and matched
SDO/HMI `hmi.M_720s` magnetograms. For each PHI frame the nearest HMI
record in time is selected and rejected if |Δt| exceeds a threshold; in
the 2025 campaign the matches are essentially simultaneous (Δt = 2–3 s).
Data are retrieved through a no-registration JSOC SUMS path
(`download_baseline_data.py`), with the necessary WCS keywords injected
into the raw segments and, for a subset of PHI files that ship without
`CUNIT1/2`, units patched on load.

Two viewing-geometry systematics are handled explicitly. First, HMI's
camera carries a ≈180° `CROTA2` rotation; in the native-geometry map path
this is honoured through the full PC/CROTA orientation, and ignoring it
mirrors the map between hemispheres and flips the sign of g₁₀ (a −2.11 →
+2.20 G swing on a two-day test subset) — a cautionary systematic for any
direct-geometry synoptic construction. Second, the line-of-sight field is
converted to an approximate radial field as B_r ≈ B_los / µ^α with a
limb cut µ ≥ µ_min; α and µ_min are exposed parameters, and their effect
is quantified by a sensitivity sweep.

Per-case cross-calibration (through-origin regression over pixels with
|B| above a floor and µ ≥ µ_min) is reported for every case as a slope
and Pearson r. In the 2025 campaign the slope is ≈0.55 with r ≈ 0.8 while
the spacecraft co-observe, and both the correlation and the naïve slope
collapse (r → 0.44, slope → 0.31) as the SolO–Earth separation grows past
~55°, because the co-observed disk shrinks toward the limb. Calibration is
applied only when explicitly requested and only when r exceeds a floor
(default 0.5), and — see §3 — only when the two spacecraft actually
co-observe.

## 3. Construction of merged magnetic maps

Three products are built on a common Carrington grid
(`run_milestone_comparison.py`), each accumulated with a
central-meridian-distance weight cos(cmd):

- **PHI-only** — SolO vantage, PHI's native disk geometry;
- **HMI-only** — Earth vantage, HMI's **native** disk geometry,
  deliberately *not* reprojected through the PHI grid, so the vantage
  comparison is not made circular;
- **merged** — a smooth radial-cosine blend of the two on the PHI grid.

The merged product is meaningful only while the two spacecraft observe a
common disk region. We therefore guard it with a maximum SolO–Earth
Carrington-longitude separation (`--max-separation-deg`, default 60°): a
case beyond the threshold contributes to the PHI-only and HMI-only
native-geometry products but is excluded from the blend and left
uncalibrated. This is not a cosmetic cut — as §4 shows, PHI's polar
advantage is largest exactly when the separation is largest, so at the
decisive epochs the merged product is legitimately empty, and the pipeline
reports that rather than fabricating a per-pixel blend of two different
hemispheres.

Because a multi-month window spans several Carrington rotations, a single
combined map smears g₁₀ across them; the campaign is therefore analysed one
rotation at a time (`run_milestone_by_rotation.py`), each rotation checked
against its own reference chart.

## 4. Results

### 4.1 Method demonstration (CR 2264, low B₀)

At CR 2264 (24 PHI cases, 2022-10-27 to 11-03) SolO led Earth in B₀ by only
~2.5°, so the window mostly tests the machinery rather than a large polar
gain. The two vantages agree on g₁₀ to ~0.05 G on their common support
(zero mode: PHI −0.18 vs HMI −0.23 G) and the maps correlate at r = 0.89,
establishing consistency. Against the standard polar-filled HMI synoptic
chart (g₁₀,ref = +0.65 G) the PHI-informed product (project mode) agrees to
+0.04 G (6%), while the same-pipeline Earth-view product misses by ~5× more,
with the accuracy ordering following polar-cap fill. Driving the SFT from
these initial conditions, the PHI-informed input evolves to an asymptotic
dipole of +3.29 G versus +1.06 G for HMI-only (factor ~3), and with a cycle
source the first polar reversal shifts by 1.26 yr. This demonstrates the
propagation mechanism; the decisive polar test requires a large B₀
advantage.

### 4.2 The 2025 high-B₀ campaign: coverage

Over 2025-02-14 to 04-29 (CR 2294–2297) Solar Orbiter's B₀ swept from
near-ecliptic (−2°) to a deep southern view (−16.7°, late March) and on to
a northern plateau (+16.8°, late April), while Earth's B₀ stayed at −5 to
−7°. The ≥60° polar-cap fill (per cent) tracks this directly:

| rotation | dates | SolO B₀ | separation | N-cap PHI/HMI | S-cap PHI/HMI |
|---|---|---|---|---|---|
| CR 2294 | Feb 14 – Mar 1 | −2 → −8° | 15–21° | 5 / 0 | 30 / 38 |
| CR 2295 | Mar 2 – Mar 28 | −8 → −17° | 0.3–60° | 0 / 0 | **64 / 46** |
| CR 2296 | Mar 31 – Apr 24 | −8 → +17° | 80–165° | **51 / 3** | 12 / 41 |
| CR 2297 | Apr 26 – Apr 29 | +16.8° apex | 168–172° | **37 / 2** | 0 / 13 |

The pattern is unambiguous and matches the hypothesis. Near the ecliptic
(CR 2294) PHI has no advantage — HMI's southern cap is even marginally
better. As SolO dives south (CR 2295) PHI's south-cap coverage overtakes
HMI (64% vs 46%). As SolO crosses to a northern view (CR 2296–2297) PHI
dominates the north cap by an order of magnitude (51% and 37% vs 3% and
2%), while, now northward-tilted, it loses the south. Because this result
is coverage — counts of observed bins — it is independent of calibration
and of the polar-fill assumption; it is the campaign's most robust finding.
[Figure: `campaign_polar_advantage.png`, from `plot_campaign_summary.py`.]

### 4.3 Vantage effect on the dipole

Where the two vantages share polar bins (CR 2295 common support) they
disagree on the *sign* of the south-polar g₁₀ contribution (project mode:
PHI +0.21 vs HMI −0.10 G on identical bins). Positive rescaling cannot flip
a sign, so this is a genuine viewing-geometry effect: HMI's south-cap B_r
there is a near-limb 1/µ^α extrapolation, unreliable near solar maximum
where the polar field is weak and reversing, whereas PHI sees the same bins
close to disk centre and yields the better-constrained term. This is the
mechanism by which a second vantage changes the inferred dipole, isolated
on bins both instruments observe.

### 4.4 SFT impact: the payoff is where Earth is blind

Because a per-pixel merge is invalid at the high-B₀ epochs, we inject PHI
as a *polar boundary constraint*: the SFT initial condition is HMI
everywhere except the cap PHI observes, where PHI's zonal field is spliced
in (`apply_polar_constraint`, `run_sft_polar_experiment.py`). Running with
a cycle source (τ = 10 yr, 22 yr integration, flux-balanced) on the pole
PHI covers each rotation:

| constraint | HMI cap fill | injected polar field | Δ first reversal |
|---|---|---|---|
| North (CR 2296) | 3% | capN = −1.51 G (HMI ≈ 0) | **−1.68 yr** |
| South (CR 2295) | 46% | capS 0.20 vs HMI 0.37 | −0.004 yr |

Constraining the north cap, where HMI is effectively blind, injects a
strong polar field where the Earth view has essentially none, flips the
initial dipole sign (g₁₀(0) = −0.26 vs +0.10 G), and pulls the first
predicted polar reversal 1.68 yr earlier. Constraining the co-observed
south cap changes nothing, because HMI already constrains that pole. In
both cases the 22-yr dipoles converge (~+3.1 G) as the deterministic
source dominates: the memory of the polar constraint lives in the reversal
*timing*, not the asymptote. The polar view moves the SFT prediction only
where the Earth view lacks the information — its impact is therefore
maximal at the high-separation northern epoch, precisely the
standalone-constraint regime.

### 4.5 Reference validation and its limit

Checked per rotation against `hmi.synoptic_mr_polfil_720s`
(`compare_reference_by_rotation.py`), the standard chart's dipole is small
and negative and grows more negative through the window (g₁₀,ref = −0.21,
−0.17, −0.18, −0.39 G for CR 2294–2297), consistent with the cycle-25 polar
field rebuilding near maximum. The partial-coverage milestone products do
*not* reproduce this near-zero signal in sign (project mode: all positive,
+0.3 to +1.0 G; the HMI-view product is nearest the reference but still off
by ~0.5 G — and since the reference is itself an HMI product, proximity to
it favours the HMI-view map by construction). Unlike the dense 8-day
CR 2264 window, a near-zero true dipole sampled from partial per-rotation
longitude coverage is swamped by sampling. We therefore treat the 2025
*absolute* g₁₀ magnitudes as provisional; the robust campaign results are
the polar coverage (§4.2) and the SFT reversal-timing differential (§4.4),
both independent of calibration and polar-fill mode.

## 5. Discussion

The organising result of the campaign is a correlation the project did not
initially anticipate: **PHI's polar advantage is largest exactly when the
SolO–Earth separation is largest**, because both are driven by the same
orbital motion — Solar Orbiter reaches high heliolatitude precisely when it
is far around the Sun from Earth. This has a sharp methodological
consequence. When the separation is small (February, and the March 13
near-conjunction) a per-pixel PHI+HMI merge is well-posed, but PHI then
carries little polar advantage; when PHI's advantage is decisive (the April
north cap) the separation is 80–172° and a merge is meaningless.

The two vantages are therefore **complementary in time, not co-registered
in space**. The scientific value of the polar view is not as a pixel-level
merge partner for HMI but as an *independent polar constraint* — a boundary
condition for the SFT model — at the epochs Earth cannot see a given pole.
The −1.68 yr reversal shift from the north-cap constraint, set against the
null result from the co-observed south cap, is the concrete expression of
this: the added vantage matters only, but decidedly, where the Earth view
is blind. The separation guard is what makes the statement rigorous, by
refusing to construct a blended product from two disjoint hemispheres.

## 6. Conclusions

1. Merged PHI+HMI synoptic maps are constructible and validated at small
   SolO–Earth separation (map-space r ≈ 0.9 between independent vantages;
   ~0.05 G common-support dipole agreement).
2. In the 2025 high-B₀ campaign PHI's polar-cap coverage overtakes HMI by
   up to 16× (north cap 51% vs 3%), and the advantage switches on with
   SolO's heliolatitude exactly as hypothesised — south cap in March, north
   cap in April.
3. PHI's polar advantage is largest where the SolO–Earth separation is
   largest, so at the decisive epochs the value of the polar view is as a
   standalone polar constraint, not a co-registered merge partner. The two
   vantages are complementary in time, not space.
4. Adding PHI's north-cap constraint at the epoch where HMI is blind (3%
   fill) shifts the first SFT-predicted polar reversal by −1.68 yr; adding
   its south-cap constraint where HMI co-observes (46% fill) changes
   nothing. The polar view moves the prediction only where the Earth view
   lacks the information.
5. The methodology, its systematics (orientation, calibration,
   deprojection, polar filling, co-observation separation), and the
   open-source tooling are established end to end, from data download
   through the polar-constrained SFT experiment.

## 7. Uncertainties and limitations

- **LOS→radial approximation.** B_r ≈ B_los/µ^α is poor on strong active-
  region fields; the quiet-Sun restriction (|B_r| ≤ 50 G) shrinks the
  cross-vantage project-mode gap, and vector inversions would remove this
  error class. The α sensitivity is quantified by `alpha_sensitivity_sweep`.
- **Calibration drift.** The per-case slope declines with SolO–Sun distance
  (0.75 at 0.42 AU → 0.50 at 0.52 AU in CR 2264) and collapses at large
  separation; a resolution-degradation test (smoothing HMI to PHI's
  per-case plate scale before the regression) is used to attribute the
  low-B₀ drift to resolution rather than vantage.
- **Coverage.** Per-rotation windows have partial longitude coverage, which
  (§4.5) dominates the uncertainty on the *absolute* 2025 g₁₀; denser PHI
  synoptic programmes would close this. Differential and coverage
  statements are robust against it.
- **Fill-mode dependence** remains the largest single uncertainty on
  absolute g₁₀; product-vs-product statements are robust against it.
- **1-D (axisymmetric) SFT.** Longitude-dependent assimilation, and
  time-dependent assimilation of PHI beyond a single initial condition, are
  future work.
